# rsu-netattach.yaml — Multus NetworkAttachmentDefinition for the vanetzalan0 bridge
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: vanetzalan0
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "type": "bridge",
      "bridge": "vanetzalan0",
      "isGateway": false,
      "ipam": {
        "type": "static"
      }
    }

diff --git a/tools/socktap/time_trigger.cpp b/tools/socktap/time_trigger.cpp
index ff8c8cac..5ce2eedc 100644
--- a/tools/socktap/time_trigger.cpp
+++ b/tools/socktap/time_trigger.cpp
@@ -9,10 +9,11 @@ namespace posix_time = boost::posix_time;
 using namespace vanetza;
 
 TimeTrigger::TimeTrigger(asio::io_service& io_service) :
-    io_service_(io_service), timer_(io_service), runtime_(Clock::at(now()))
+    io_service_(io_service), timer_(io_service), sync_timer_(io_service), runtime_(Clock::at(now()))
 {
     //std::cout << "Starting runtime at " << now() <<"\n";
     schedule();
+    schedule_sync();
 }
 
 posix_time::ptime TimeTrigger::now() const
@@ -41,6 +42,20 @@ void TimeTrigger::on_timeout(const boost::system::error_code& ec)
     }
 }
 
+void TimeTrigger::schedule_sync()
+{
+    sync_timer_.expires_from_now(boost::posix_time::milliseconds(10));
+    sync_timer_.async_wait(std::bind(&TimeTrigger::on_sync_timeout, this, std::placeholders::_1));
+}
+
+void TimeTrigger::on_sync_timeout(const boost::system::error_code& ec)
+{
+    if (asio::error::operation_aborted != ec) {
+        schedule();       // advance runtime to wall clock, reschedule app event timer
+        schedule_sync();  // arm next 10ms sync pulse
+    }
+}
+
 void TimeTrigger::update_runtime()
 {
     auto current_time = now();
diff --git a/tools/socktap/time_trigger.hpp b/tools/socktap/time_trigger.hpp
index 2dc00a9b..fad710c4 100644
--- a/tools/socktap/time_trigger.hpp
+++ b/tools/socktap/time_trigger.hpp
@@ -18,12 +18,15 @@ public:
 
 private:
     void on_timeout(const boost::system::error_code&);
+    void on_sync_timeout(const boost::system::error_code&);
+    void schedule_sync();
     void update_runtime();
 
     boost::asio::io_service& io_service_;
     boost::asio::deadline_timer timer_;
+    boost::asio::deadline_timer sync_timer_;
     vanetza::ManualRuntime runtime_;
-    std::recursive_mutex schedule_mtx; 
+    std::recursive_mutex schedule_mtx;
 };
 
 #endif /* TIME_TRIGGER_HPP_XRPGDYXO */
